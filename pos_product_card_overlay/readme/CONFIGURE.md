No configuration is required.

Install or upgrade the module from **Apps** (technical name: `pos_product_card_overlay`).
Ensure product images and optional **Color** on the product template (Point of Sale tab)
are set as usual in Odoo; this module only changes how labels are rendered on image cards.
