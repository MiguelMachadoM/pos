## 18.0.1.0.0

- Per POS shop name, address, phone and email on the receipt header. ZIP
  autocomplete via ``base_location`` fills city and province. Compact thermal
  header with phone/email icons. Independent from ``pos_config_logo``.
  Spanish simplified invoice layout is a separate addon:
  ``l10n_es_pos_config_shop_identity`` in OCA/l10n-spain.
