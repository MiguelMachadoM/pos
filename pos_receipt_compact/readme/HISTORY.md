## 18.0.1.0.0

- Compact POS receipts: per-section font sizes, smaller or hidden order
  number, single-line product names (ellipsis / configurable character
  fallback, default 60; 0 skips JS truncation), hide the unit-price row
  when quantity is 1, tighter padding, bold TOTAL.
